---
import moment from 'moment';

interface Props {
	date: Date;
	format?: string;
}

const { date, format = "YYYY-MM-DD", ...props } = Astro.props;
---

<time datetime={date.toISOString()} {...props}>
	{ moment(date).format(format) }
</time>
